@{
    // The code within the ViewStart file is executed before any page that it affects,
    // which is any Razor page in the same folder and all subfolders.

    // ViewStart is executed before any pages it affects,
    // making it the ideal way to set the default layout for all pages (at this level in the folder/file hierarchy).
    Layout = "_Layout";
}
